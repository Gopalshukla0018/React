# Project Title

## Branch Renaming

The default branch has been renamed to `main`. Use the following commands to set the upstream branch:

```sh
# Check the current branch name
git branch

# Rename the current branch to main
git branch -M main

# Push the renamed branch to the remote repository
git push -u origin main
```
